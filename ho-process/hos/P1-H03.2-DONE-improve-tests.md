# TASK: Improve llm-client Test Coverage

## GOAL

Fill test coverage gaps identified during Ho 03 audit. No new features — tests only.

## SCOPE

`packages/llm-client/tests/` only. Do not modify any source files.

## GAPS TO FIX

### 1. Factory tests for Narrator and ActionInterpreter (MISSING)

`create_narrator` and `create_action_interpreter` factories exist but have minimal/no test coverage.

Add tests mirroring the existing `create_case_generator` tests:

```python
# test_factory_narrator.py

def test_create_narrator_mock_returns_mock_narrator():
    """Factory with MOCK provider returns MockNarrator."""

def test_create_narrator_openai_not_implemented():
    """Factory with OPENAI provider raises — no real Narrator provider in Phase 1."""

def test_create_narrator_anthropic_not_implemented():
    """Factory with ANTHROPIC provider raises — no real Narrator provider in Phase 1."""

def test_create_action_interpreter_mock_returns_mock():
    """Factory with MOCK provider returns MockActionInterpreter."""

def test_create_action_interpreter_openai_not_implemented():
    """Factory with OPENAI provider raises — no real ActionInterpreter in Phase 1."""
```

### 2. Mock case dict validation against actual schema (WEAK)

Current test checks for 8 top-level keys. It should validate the mock dict against the actual JSON schema to ensure the mock stays honest.

```python
# test_mock_schema_conformance.py
import json
from jsonschema import validate

def test_mock_case_conforms_to_schema():
    """MockCaseGenerator output validates against case-definition.schema.json."""
    schema_path = Path(__file__).parents[3] / "schemas" / "case-definition.schema.json"
    schema = json.loads(schema_path.read_text())
    generator = MockCaseGenerator(mock_config)
    case = generator.generate_case(seed)
    validate(instance=case, schema=schema)  # Raises on failure
```

### 3. Error wrapping edge cases (THIN)

Add tests for:

```python
# test_error_handling.py

def test_openai_timeout_wrapped_as_provider_error():
    """Network timeout from openai SDK becomes LLMProviderError."""

def test_anthropic_timeout_wrapped_as_provider_error():
    """Network timeout from anthropic SDK becomes LLMProviderError."""

def test_openai_invalid_json_response_wrapped_as_response_error():
    """Non-JSON response from OpenAI becomes LLMResponseError."""

def test_anthropic_invalid_json_response_wrapped_as_response_error():
    """Non-JSON response from Anthropic becomes LLMResponseError."""

def test_openai_empty_response_wrapped_as_response_error():
    """Empty/null response becomes LLMResponseError."""
```

### 4. Boundary type construction (MISSING)

No tests verify that `CaseSeed`, `GameEvent`, `GameStateSnapshot`, `ExplanationContext`, `ParsedAction` can be constructed with expected fields.

```python
# test_boundary_types.py

def test_case_seed_fields():
    seed = CaseSeed(diagnosis="pneumonia", difficulty="intermediate", dramatic_tone="clinical")
    assert seed.diagnosis == "pneumonia"
    assert seed.difficulty == "intermediate"

def test_parsed_action_fields():
    action = ParsedAction(action_type="examine", target="chest", confidence=0.95, raw_input="listen to lungs")
    assert action.action_type == "examine"
    assert action.confidence == 0.95

def test_game_event_fields():
    event = GameEvent(event_type="vital_change", data={"hr": 120}, timestamp="2026-01-01T00:00:00Z")
    assert event.event_type == "vital_change"
```

### 5. MockActionInterpreter behavior (THIN)

Current tests likely only check return type. Add behavior tests:

```python
# test_mock_action_interpreter.py

def test_mock_parse_returns_action_type_from_input():
    """Mock parses simple strings into reasonable action types."""

def test_mock_parse_unknown_input_returns_default():
    """Unknown input still returns a valid ParsedAction, not an error."""

def test_mock_parse_empty_string():
    """Empty string input handled gracefully."""
```

## CONSTRAINTS

- No source code changes
- No new dependencies except `jsonschema` (add to dev dependencies if missing)
- All tests must pass without API keys
- Use `unittest.mock.patch` for SDK error simulation — never call real APIs
- Every test has a docstring explaining what it verifies
- Every assert is meaningful (no `assert True`)

## ACCEPTANCE

```bash
cd packages/llm-client
pytest tests/ -v          # All pass, 0 failures
pytest tests/ --tb=short  # No warnings about missing markers
```

Expected: ~75-85 total tests (up from 60), all passing in < 2 seconds.

## COMMIT

```
test(llm-client): fill coverage gaps for factories, schema conformance, error wrapping, boundary types
```
