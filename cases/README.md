# Cases

This directory contains frozen, validated medical case artifacts.

Cases are generated by Anamnesis, validated against JSON schemas, and stored here as immutable JSON files. Satori loads these frozen cases during gameplay to ensure consistent, reproducible investigations.

Each case file represents a complete medical mystery with:
- Patient information
- Medical history
- Available tests and examinations
- Diagnostic truth
- Investigation constraints
